# Constraints to keep pip resolver from excessive backtracking.
# These pins are chosen to be compatible with Python 3.10 + tensorflow==2.11
# and the legacy t5==0.9.3 dependency chain.

# t5==0.9.3 depends on tfds-nightly; pin to a specific build to avoid pip
# trying hundreds of nightly variants.
tfds-nightly==4.9.9.dev202510250044

# Keep tensorflow-datasets' metadata deps stable.
tensorflow-metadata==1.13.1
googleapis-common-protos==1.63.1

# seqio pulls jax/flax/optax; pin a known-good CPU stack for py310.
jax==0.4.35
jaxlib==0.4.35

# Pin seqio / flax ecosystem to avoid further resolver churn.
seqio==0.0.20
clu==0.0.12
flax==0.10.4
optax==0.2.6
chex==0.1.82
orbax-checkpoint==0.11.33

# t5 eval deps
sacrebleu==2.6.0
rouge-score==0.1.2
portalocker==3.2.0

# Reduce resolver branching for librosa stack on py310 + numpy==1.23.5
scipy==1.11.4
scikit-learn==1.2.2
soundfile==0.12.1

# Keep build backend stable (pkg_resources present)
setuptools==80.9.0

# Further reduce branching in common transitive deps
requests==2.32.5
urllib3==2.6.3
certifi==2026.2.25
idna==3.11
charset-normalizer==3.4.6

PyYAML==6.0.2
regex==2026.2.19

pandas==2.3.3
python-dateutil==2.9.0.post0
pytz==2026.1.post1

google-auth==2.49.1
google-auth-oauthlib==0.4.6
requests-oauthlib==1.3.1
pyasn1==0.6.2
pyasn1-modules==0.4.2
rsa==4.9.1

# sentencepiece must be compatible with protobuf==3.19.6
sentencepiece==0.1.99

# evaluation
mir_eval==0.8.2
